{
  "name": "crash-lens-server",
  "version": "1.0.0",
  "private": true,
  "description": "CRASH LENS API server — Qdrant proxy, Brevo email, R2 upload, Stripe payments",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "firebase-admin": "^12.0.0",
    "node-cron": "^3.0.0",
    "stripe": "^14.0.0"
  }
}
